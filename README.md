# Spring-Core-XML-Dependency-Injection
This program is about Spring Core — the foundation of the Spring Framework. It teaches the basic idea of how Spring automatically creates and manages objects (beans) using an XML configuration file.
